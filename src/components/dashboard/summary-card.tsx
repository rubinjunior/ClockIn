import type { LucideIcon } from "lucide-react";
export function SummaryCard({ icon: Icon, label, value, detail, tone = "primary" }: { icon: LucideIcon; label: string; value: string; detail?: string; tone?: "primary" | "success" | "warning" }) { const color = tone === "success" ? "var(--success)" : tone === "warning" ? "var(--warning)" : "var(--primary)"; return <article className="card min-w-0 p-4"><div className="mb-4 grid size-10 place-items-center rounded-2xl bg-[var(--primary-soft)]" style={{ color }}><Icon aria-hidden size={20}/></div><p className="muted truncate text-sm">{label}</p><p className="metric-value mt-1 text-xl font-extrabold">{value}</p>{detail && <p className="muted mt-1 text-xs">{detail}</p>}</article>; }
